its about AND operation  
in vc
#! /bin/bash

age=25
if [ "$age" -gt 18 ] && [ "$age" -lt 30 ] 
then 
   echo "valid age"
   else
      echo "age not valid" 
  fi

save it on vc ctrl+s

now in terminal
rake@rake-VirtualBox:~$ cd Desktop
rake@rake-VirtualBox:~/Desktop$ ./hello.sh
valid age
 its show you valid

 #! /bin/bash

age=50
if [ "$age" -gt 18 ] && [ "$age" -lt 30 ] 
then 
   echo "valid age"
   else
      echo "age not valid" 
  fi
  
  save ctrl+s in vc 
  
now in terminal
rake@rake-VirtualBox:~/Desktop$ ./hello.sh
age not valid
  
  suppose if age 50 its you show not valid
  
  
  
  #! /bin/bash

age=50
if [ "$age" -gt 18 -a "$age" -lt 30 ] 
then 
   echo "valid age"
   else
      echo "age not valid" 
  fi
  ctrl+s save in vc 
  by using -a its greater then 

rake@rake-VirtualBox:~/Desktop$ ./hello.sh
age not valid
in terminal its shows you


#! /bin/bash

age=25
if [ "$age" -gt 18 -a "$age" -lt 30 ] 
then 
   echo "valid age"
   else
      echo "age not valid" 
  fi
ctrl+s save in vc 


rake@rake-VirtualBox:~/Desktop$ ./hello.sh
valid age

in terminal its shows you vaild age changing the value 50- 25
  
  
  
  
